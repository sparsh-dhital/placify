{
  "name": "placify",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names backend,frontend --prefix-colors blue,green \"cd backend && python -m uvicorn main:app --reload --port 8000\" \"npm --prefix frontend run dev -- --port 5174\"",
    "dev:backend": "cd backend && python -m uvicorn main:app --reload --port 8000",
    "dev:frontend": "npm --prefix frontend run dev -- --port 5174"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
